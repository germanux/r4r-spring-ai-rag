# Current state

- Active task: `base-catalog-green`.
- Phase 0 excludes database, REST, frontend and LLM calls.
- Expected gate: Java 21 and `mvn test` green.
- Next action: run the deterministic gate before requesting any code change.
