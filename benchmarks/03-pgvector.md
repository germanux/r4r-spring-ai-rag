# Benchmark 03 — PostgreSQL and pgvector

Add only after Benchmark 02 is green:

- PostgreSQL/pgvector schema managed by Flyway;
- one repository boundary;
- real PostgreSQL integration test;
- dimension validation;
- deterministic replacement and rollback.

Do not add REST or Angular in this phase.
