# Benchmark 02 — deterministic ingestion

Add only after Benchmark 01 is green:

- SHA-256 per source document;
- explicit ingestion fingerprint;
- unchanged-source no-op;
- bounded metadata;
- focused unit and integration tests.

No vector database yet.
