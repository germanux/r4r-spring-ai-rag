# Benchmark 04 — Spring AI RAG

Add only after Benchmark 03 is green:

- Spring AI abstractions for embeddings and chat;
- no handwritten Ollama HTTP clients;
- retrieval with source identifiers;
- grounded answer prompt;
- citation and abstention tests.

A thin REST endpoint may be added only after the service contract is green.
