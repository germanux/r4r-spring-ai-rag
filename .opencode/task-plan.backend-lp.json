{
  "schema_version": 1,
  "queue_id": "backend-lp",
  "phase": "backend",
  "worker": "LP",
  "source_plan": ".opencode/task-plan.backend.json",
  "tasks": [
    {
      "id": "task-06f-ingestion-validation",
      "command": ".opencode/commands/task-06f-ingestion-validation.md",
      "objective": "Complete the full deterministic production-ingestion CLI validation after task-06e is integrated.",
      "allowed_paths": ["pom.xml", "src/main/**", "src/test/**", "docs/backend/**", ".env.example"],
      "gate": ["./scripts/task-gate.sh", "task-06f-ingestion-validation"],
      "commit_message": "test(ingestion): complete production CLI validation"
    },
    {
      "id": "task-08-rag-semantic-evaluation",
      "command": ".opencode/commands/task-08-rag-semantic-evaluation.md",
      "objective": "Add grounded retrieval, citation and abstention evaluation for the populated RAG.",
      "allowed_paths": ["pom.xml", "src/main/**", "src/test/**", "docs/backend/**"],
      "gate": ["bash", "-lc", "rm -rf target && ./scripts/task-gate.sh all && mvn -Dtest=RagSemanticEvaluationTest -DfailIfNoTests=true test"],
      "commit_message": "test(task-08): add semantic RAG evaluation"
    }
  ],
  "final_gate": ["./scripts/task-gate.sh", "all"]
}
