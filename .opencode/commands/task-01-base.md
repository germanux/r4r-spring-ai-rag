# Task 01 — Base

## Outcome

- Java 21 non-web Spring Boot application compiles/packages.
- Using the current backend, we need to create an API REST of the current java code for chunking, document, rag, service and vector.
- Just select the only one three most important RestControllers...
- When you have only one REST java controller, as soon you should create the Angular app with ng new frontend/ 
- The you should continue like this: Creatin a method, adding the angular component or feature to call this endpoint.
- For each endpoints group, in fact, for all common endpoints, it would be a http request in an angular service, and being used by any component.

- Do not add product features, REST, frontend, Playwright, Testcontainers or handwritten
Ollama HTTP clients.

Gate: `./scripts/task-gate.sh task-01-base`
