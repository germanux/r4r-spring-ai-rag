Read the newest decision under `runtime/runs/*/decisions/`, then resume only the exact
next action it authorizes. Re-read the active benchmark, not every command. Do not infer
approval from a green build alone. Do not commit or push.
