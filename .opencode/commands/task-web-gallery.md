# Web task — before/after gallery section

Reference: `https://riansares4r.com/galeria-antes-despues`.
Use `/html/body/main/section[2]` only as a hint; confirm the heading
`Trabajos realizados` before editing.

Open the public page read-only with Playwright. Inspect the target DOM, computed CSS,
responsive layout, console and only directly relevant CSS/JS requests. Locate the
local route/source; if absent, stop instead of creating a parallel site.

Preserve the hero and every later section. Rebuild only the local gallery section,
using local components/tokens. Do not copy full stylesheets, minified bundles,
analytics, trackers, cookies or credentials. Do not submit forms, authenticate,
deploy, push or mutate the remote site.

Run the existing formatter/build/tests and available browser checks. Stop after one
coherent implementation and validation pass. Report changed files, checks, visual
differences and the first unproven item.
