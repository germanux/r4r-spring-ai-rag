# FE-04 — Deterministic Playwright verification

LP/30B only. Edit `frontend/**` and `docs/frontend/**` only.

Add Playwright configuration and tests for: successful cited answer, abstention, API
error, keyboard submission and visible ordered citation metadata. Stub the HTTP endpoint
inside Playwright; tests must not require a live LLM. Keep Angular major 17.

Run exactly `./scripts/frontend-task-gate.sh task-fe-04-playwright`.
